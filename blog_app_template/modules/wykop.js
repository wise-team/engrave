module.exports.wykopAddLink = function (link, title, body, tags) {
    console.log(link, title, body, tags);    
}

module.exports.wykopMirkoAdvert = function (wykop_url) {
    console.log(wykop_url);
}